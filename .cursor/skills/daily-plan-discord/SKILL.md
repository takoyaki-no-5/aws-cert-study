---
name: daily-plan-discord
description: >-
  今日の学習予定を組み立てて提示し、Discord に送る（調整・追加・スキップ対応）。
  Use when the user says 今日の予定, Discord, 通知, デイリー, daily plan,
  予定を送って, or wants today's study plan posted to Discord.
---

# 今日の予定 → Discord

固定タスク: **今日の予定を出す →（必要なら調整）→ Discord に送る**。

## 前提

- 1日の学習枠: **`study/profile.md` の平日 2時間40分（160分）**（変更があれば profile を正とする）
- Webhook: Cloud Secret / env **`discord_daily_bot`**
- 送信: `python3 .cursor/hooks/discord-notify.py`（URL をログに出さない）
- Anki はテキスト全部のあと（最後）。通勤 Anki は別枠可だが、合計が 160分を超えるなら明示して聞く

## 手順（開始）

1. 読む: `study/profile.md` / `study/schedule.md` → 進行中 `study/<資格>/schedule.md` / 今日の `days/YYYY-MM-DD.md` / 直近 `study/log/` / 必要なら `books.md`
2. 今日の day が無ければ、未消化・roadmap・残り日数から **160分以内** の案を作る（まだ確定・送信しない）
3. **ズレ検知**（どれか当てはまったら、勝手に押し込めず **先に質問**）:
   - 前日以前の未完が残っている
   - 残り工数 ≫ 残り日数 × 160分
   - day の合計が 160分を大きく超える／足りない
   - 受験日・リスケ判定日が近いのに進捗が薄い
4. チャットに短く提示（資格・日付・ブロック表・合計分・次に始める1手）
5. ユーザーが OK / 無言で進めてよい流れなら Discord 送信。調整指示があれば下の「調整」へ

## Discord メッセージ（短く）

```
📅 YYYY-MM-DD（曜日） <資格略称>
合計: NN分 / 160分

1. HH:MM〜 題材（分）
2. …
最後: Anki（分）

いまやること: …
```

送信例:

```bash
python3 .cursor/hooks/discord-notify.py "$(cat <<'EOF'
📅 ...
EOF
)"
```

成功後、チャットに「Discord 送信済み」と一言。失敗時は env 未設定かネットワークを疑い、予定自体はチャットに残す。

## 調整（「これもやる」「これはやらない」）

ユーザー指示を day / 予定に反映してから、必要なら Discord を**再送**（訂正と分かる一文を先頭に）。

| 指示 | 動き |
|------|------|
| やらない・スキップ | そのブロックを外す。空いた分を次の優先題材に振るか、短縮してよいか聞く |
| これもやる・追加 | **想定時間が分かる**（roadmap・章の目安・ユーザー明示）→ 160分に収まる案を出して確認。**分からない** → **分数を聞いてから** day 更新・送信・学習開始しない |
| 順番変更 | 合計160分と「テキスト→Anki最後」を崩さない範囲で並べ替え |

### 質問してから動く（必須）

次は **推測で確定しない。質問 → 返答待ち**:

- 追加タスクの所要時間が不明
- 追加すると 160分超え／後続日にズレ込みそう
- 削ると合格リスクが変わりそう（範囲丸ごと落ちる等）
- リスケ判定や受験日に影響しそう

聞いてよい例:「追加分は何分想定？ 30 / 60 / 不明」「160分超えるので A を明日に回す？それとも今日伸ばす？」

## ファイル更新

- 確定した予定 → `study/<資格>/days/YYYY-MM-DD.md`（無ければ作成。題材・短文）
- 大きな配分変更 → 進行中 `schedule.md` の該当日
- 終わったら / 切り上げたら `study/log/YYYY-MM-DD.md`（既存の日次セッションスキルと同様）

## やらない

- Webhook URL・シークレットをコミット・チャット全文に貼る
- 時間不明の追加を黙って押し込む
- ズレを黙って後日付に玉突きする（必ず明示して確認）
- 学習範囲を独断で削る（ユーザー承認が必要）

## 関連

- セッション進行（ブロック消化・ログ）: `daily-study-session`
- 計画の大きな組み直し: `study-plan-manager`
