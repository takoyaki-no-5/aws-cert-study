---
name: study-plan-manager
description: >-
  AWS資格の学習計画・週次/日次スケジュールを作成・改定する。Use when the user asks for
  計画, スケジュール, カリキュラム, 期限, 週次計画, replanning after falling behind,
  or study plan changes.
---

# 学習計画マネージャ

## 入出力ファイル

以下を読み、更新する:

- `study/profile.md` — 資格・受験日・週の時間・レベル
- `study/plan.md` — 全12資格の受験日・**必要工数**・週負荷（楽〜厳しい）。受験日固定・週数で割った結果の負荷差は許容
- `study/README.md` — 階層の地図
- `study/schedule.md` — 薄いハブ（今どの試験か + 全資格リンク。日次の実体は書かない）
- `study/<資格>/exam.md` + `roadmap.md` + `schedule.md` + `days/` — 資格ごとの実体（規約は `AGENTS.md`。見本: `saa/`）

## 資格の立ち上げ手順（立ち上げ日曜にやる）

1. `study/<資格>/` は全12資格分を用意済み。無ければ `_templates/` から作成（略称: clf, saa, scs, sap, ans, dva, soa, dop, dea, aif, mla, aip）。
2. `exam.md` を埋める（または確認）: **全資格同一フォーマット**（`_templates/exam.md`）。時間・問題形式・合格点・学習時間（一般 vs 今回）。
3. `roadmap.md` を確認・厚くする: **題材のみ**（見本: `study/clf/roadmap.md`）。順番と工数 → ドメイン → トピック → 短いチェック。範囲は削らない。運用は書かない。
4. `schedule.md`: **運用のみ・短文**。フェーズ・日次をその週に確定。合格点の詳細は `exam.md`。
5. `days/` は当日（または前夜）生成。ゴール・時間割・題材チェックのみ。
6. `study/anki/<資格>/` に工程フォルダを切る。`study/schedule.md` ハブの「今」を更新。
7. 資格フォルダ間でファイル構成の差分を増やさない（`study/README.md` 正）。

## 進め方

1. **受験日は動かさない**（廃止期限・全冠ゴール）。確保できる週数は前試験翌日〜受験日。
2. 工数は「週×33h」ではなく**その試験に必要な量**（深さ・転送・新領域）。週数で割って楽/厳しいが分かれてよい。
3. **合格最短**: 実務・実力育成は無視。配点・頻出・誤答になりやすい区別を優先。ハンズオンは入れない（ユーザーが求めたときのみ）。
4. **受験順**: 内容が近いものを連続。現行順と理由は `study/plan.md` を正とする。
5. 経験のある分野も範囲から外さず、時間配分の重みだけ弱点側に寄せる。
6. 公式 Exam Guide のドメイン配点で夜のインプットを配分（最重量に厚く）。
7. 試験前に模試①。1週集中の資格（AIF 等）は型を短くしてよい。
8. 日本語で、ブロックごとの分数/時間まで具体的に。

## 日次枠

- 平日の学習枠は **`study/profile.md` の 帰宅後 2時間40分 + 電車 Anki 30分** を正とする。
- 日次の提示・Discord 通知・その場の追加/スキップはスキル **`daily-plan-discord`**。
- 残り日数×枠で足りない分は詰め込まず、資格 `schedule.md` に **不足** として残す（合格前提の優先順は守る）。

## 遅れたとき

- 試験日を後ろにずらす前に、削る・統合する候補トピックを**提案してユーザーが決める**（勝手に削らない）。
- 周辺サービスの追加より模試日を守ることを優先する。
- 追加タスクの所要が不明、または後続にズレ込みそうなときは **質問してから** 組み直す。
