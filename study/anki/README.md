# Anki TSV

資格ごとに階層管理する。

| パス | 内容 |
|------|------|
| `study/anki/clf/` | CLF（工程フォルダ + TSV） |
| `study/anki/<資格>/` | 今後の資格も同様 |

形式: UTF-8 / Tab / ヘッダなし / `表面` `裏面` `タグ`

投入: AnkiConnect（`anki-add.py`）優先。詳細は各資格フォルダの README。
