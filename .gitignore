# Personal / sensitive context (repo is public)
.private/

# Local downloads / scratch
.tmp/

# Local / secret ignores for an AWS workspace
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
*.key
credentials.json
secrets.json
secrets.yaml
secrets.yml
.aws/
**/aws_credentials
